var Backbone = require("backbone");
var $ = require("jquery");
Backbone.$ = $;

module.exports = Backbone.Model.extend({
	defaults: {
		id: null,
		title: null,
		description: null,
		itemCondition:
		category: 
		location:
		userID:
		userName:
		userEmail:
		userPhone:
		userAddress:
		userZip:
		itemImage:
	}
});